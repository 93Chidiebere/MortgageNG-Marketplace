services:
  - type: web
    name: mortgage-backend
    env: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: MONGO_URI
        value: mongodb+srv://mortgage_db:jmODRCsfVRFqPODl@mortgagecluster.vamezbl.mongodb.net/?appName=MortgageCluster
